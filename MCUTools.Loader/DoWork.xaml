<UserControl x:Class="MCUTools.Loader.DoWork"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Current Progress:" Grid.Row="0" VerticalAlignment="Center" Margin="10,0,0,0"/>
        <ProgressBar x:Name="PbCurrent" Grid.Row="1" Minimum="0" Maximum="100" Value="0" IsIndeterminate="True" Height="30" Margin="20,0"/>
        <TextBlock Text="Operation Progress:" Grid.Row="2" VerticalAlignment="Center" Margin="10,0,0,0"/>
        <ProgressBar x:Name="PbComplete" Grid.Row="3" Height="30" Margin="20,0"/>
    </Grid>
</UserControl>
